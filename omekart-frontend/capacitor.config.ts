import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.omekart.app',
  appName: 'Omekart',
  webDir: 'out',
  server: {
    url: 'https://omekart-frontend-seyistarrs-projects.vercel.app'
  }
};

export default config;
